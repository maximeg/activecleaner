before_install:
  - gem update --system
  - gem install bundler
bundler_args: --without local
gemfile:
  - gemfiles/rails42.gemfile
  - gemfiles/rails50.gemfile
  - gemfiles/rails51.gemfile
language: ruby
rvm:
  - 2.3.8
  - 2.4.5
  - 2.5.3
  - 2.6.0
script:
  - bundle exec rake ci
